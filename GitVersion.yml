mode: ContinuousDeployment
branches:  
   master:  
      regex: ^master$|^main$
   xm:
      regex: ^fork/xm$
      mode: ContinuousDeployment
      increment: None
      tag: "xm"
      source-branches: []